    $( document ).on( 'keydown', function ( e ) {
		var form = document.querySelector('form');
    var aramaAlani = document.getElementById("userinput");
      if ( e.keyCode === 27 ) {
        aramaAlani.value = '';
        aramaAlani.focus();     
       
      }
    }); 
	$( document ).on( 'keydown', function ( e ) {
      if ( e.keyCode === 48 ) {
        window.location.replace("#0")      
      }
    });
	$( document ).on( 'keydown', function ( e ) {
      if ( e.keyCode === 49 ) {
        window.location.replace("#1")     
      }
    });$( document ).on( 'keydown', function ( e ) {
      if ( e.keyCode === 50 ) {
        window.location.replace("#2")     
      }
    });$( document ).on( 'keydown', function ( e ) {
      if ( e.keyCode === 51 ) {
        window.location.replace("#3")     
      }
    });$( document ).on( 'keydown', function ( e ) {
      if ( e.keyCode === 52 ) {
        window.location.replace("#4")     
      }
    });$( document ).on( 'keydown', function ( e ) {
      if ( e.keyCode === 53 ) {
        window.location.replace("#5")     
      }
    });$( document ).on( 'keydown', function ( e ) {
      if ( e.keyCode === 54 ) {
        window.location.replace("#6")     
      }
    });$( document ).on( 'keydown', function ( e ) {
      if ( e.keyCode === 55 ) {
        window.location.replace("#7")     
      }
    });$( document ).on( 'keydown', function ( e ) {
      if ( e.keyCode === 56 ) {
        window.location.replace("#8")     
      }
    });$( document ).on( 'keydown', function ( e ) {
      if ( e.keyCode === 57 ) {
        window.location.replace("#9")     
      }
    });

